🎯 Phase 3 生产硬化·三波串行派单（质量优先版） —— 不计时间成本，只求硬核质量

---

📋 饱和攻击头部

火力配置：1 Agent 串行（Engineer，质量优先模式）

轰炸模式：3 Waves 串行饱和攻击，前波不过后波不开

目标：Wave 1(A) → Wave 2(B) → Wave 3(C) 阶梯冲刺，每波10项红线，违反即停，不计时间成本，只求生产级质量

输入基线：ID-204 v2.7.0-HELL-A级（三债清零，100K测试通过）

---

🔍 输入基线（完整技术背景·零占位符）

输入项	强制要求	验证命令	状态	
Git坐标	`fix/hell001-debt-clearance` 创建 `feat/phase3-prod-hardening`	`git checkout -b feat/phase3-prod-hardening`	必须	
基线版本	v2.7.0-HELL-A级	`npm test` 116 passed	必须	
质量约束	不计时间成本，只求质量，前波不过后波不开	声明	必须	
实用主义边界	3分钟模拟（非7×24），Windows/Linux（非macOS）	声明	必须	

---

🎯 工单矩阵（串行铁律·质量优先）

🔥 Wave 1：选项A - 3分钟压力测试硬化（质量门）

角色：Engineer（质量优先模式）

目标：3分钟高频压力测试，检测内存泄漏，RSS增长<30%（严格于之前的50%），建立生产级稳定性基线

阻塞后续：Wave 1验收通过前，禁止启动Wave 2

输出交付物：

交付物1：3分钟压力测试（3min-stress.test.ts）
- 文件路径：`tests/stress/3min-stress.test.ts`
- 行数限制：100行±10行（质量优先，允许更严谨）
- 必须包含的正则：
  - `for.*let.*i.*<.*180000`（3分钟循环，高频操作）
  - `process.memoryUsage().rss`（内存采样）
  - `growth.*<.*0.3`（30%增长红线，严格）
  - `console.table.*memory`（可观测输出）
- 禁止包含：`setTimeout`简化模拟、mock内存数据、<3分钟缩减

交付物2：内存分析工具（memory-profiler.mjs）
- 文件路径：`scripts/dev/memory-profiler.mjs`
- 行数限制：80行±10行
- 功能：每秒采样RSS，生成CSV，计算泄漏斜率，3分钟后输出诊断报告

《刀刃》风险自测表（16项质量版）：

类别	编号	自测项	验证命令（可复制）	通过标准	状态	
FUNC	001	3分钟完整运行不崩溃	`timeout 200 npm test -- tests/stress/3min-stress.test.ts`	exit 0	[ ]	
FUNC	002	RSS增长<30%	测试输出`growth: 23%`（示例）	<30%	[ ]	
FUNC	003	泄漏斜率计算	profiler输出`slope < 0.1 MB/s`	<0.1	[ ]	
CONST	001	高频操作循环	`grep -c "addVector\|query" tests/stress/3min-stress.test.ts`	≥100处	[ ]	
NEG	001	边界：空向量压力	测试包含`null/undefined`处理	不崩溃	[ ]	
NEG	002	边界：超大向量压力	测试包含`10MB`向量	正常处理	[ ]	
NEG	003	回归：原116测试不破	`npm test`	116 passed	[ ]	
NEG	004	异常：强制GC后内存	`global.gc()`后RSS下降	可下降	[ ]	
E2E	001	3分钟连续读写	无中断	完成	[ ]	
E2E	002	分片切换压力	跨分片操作	无泄漏	[ ]	
E2E	003	LRU淘汰压力	高频evict	无异常	[ ]	
High	001	明显泄漏检测（>50%）	增长>50%时测试失败	敏感	[ ]	
High	002	内存曲线平滑	无锯齿状异常	平滑	[ ]	

地狱收卷红线（10项质量优先）：
违反任一项即返工/失败，禁止进入Wave 2：

1. RED-W1-001：RSS增长≥30%（质量不达标）→ 失败
2. RED-W1-002：测试运行<3分钟（未充分压力）→ 失败
3. RED-W1-003：3分钟内崩溃/OOM → 失败
4. RED-W1-004：原116测试被破坏 → 失败
5. RED-W1-005：无内存采样数据 → 失败
6. RED-W1-006：泄漏斜率>0.5 MB/s（快速泄漏）→ 失败
7. RED-W1-007：空向量处理崩溃 → 失败
8. RED-W1-008：未检测强制GC响应 → 失败
9. RED-W1-009：代码<80行（过于简单）→ 失败
10. RED-W1-010：mock内存数据（非真实采样）→ 失败

Wave 1验收标准：
- 13项刀刃全绿
- 10项红线零触发
- 3分钟测试输出完整内存报告
- 验收口令："Wave 1 A级/Go，启动Wave 2"

---

🔥 Wave 2：选项B - CI/CD硬化 + 跨平台（依赖Wave 1）

角色：Engineer（质量优先模式）

目标：GitHub Actions双平台（Windows/Linux），覆盖率门禁≥80%，性能回归10%阻断，生产级CI防护

前置条件：Wave 1验收A级/Go

阻塞后续：Wave 2验收通过前，禁止启动Wave 3

输出交付物：

交付物1：CI配置（hardened-ci.yml）
- 文件路径：`.github/workflows/hardened-ci.yml`
- 行数限制：120行±10行（质量优先，允许复杂配置）
- 必须包含的正则：
  - `runs-on:.*ubuntu-latest`（Linux）
  - `runs-on:.*windows-latest`（Windows）
  - `fail-under.*80`（覆盖率门禁）
  - `regression.*10%`（性能回归检测）
  - `max-old-space-size`（内存限制）

交付物2：跨平台适配层（platform-adapter.ts）
- 文件路径：`src/utils/platform-adapter.ts`
- 行数限制：80行±10行
- 必须处理：路径分隔符、换行符、文件锁、临时目录、权限差异

交付物3：回归门禁（regression-gate.mjs）
- 文件路径：`scripts/ci/regression-gate.mjs`
- 行数限制：100行±10行
- 功能：基准对比、10%阈值阻断、历史趋势、详细报告

《刀刃》风险自测表（16项质量版）：

类别	编号	自测项	验证命令	通过标准	状态	
FUNC	001	Linux CI通过	Actions日志	✅	[ ]	
FUNC	002	Windows CI通过	Actions日志	✅	[ ]	
FUNC	003	覆盖率门禁生效	PR模拟	<80%阻断	[ ]	
FUNC	004	性能回归门禁生效	模拟10%慢	阻断	[ ]	
CONST	001	Node 18/20双版本	matrix	命中	[ ]	
CONST	002	内存限制512MB	配置	命中	[ ]	
NEG	001	恶意PR阻断测试	故意降速15%	阻断	[ ]	
NEG	002	覆盖率<80%阻断	删除测试	阻断	[ ]	
NEG	003	回归：原116测试	CI日志	116 passed	[ ]	
E2E	001	跨平台路径测试	Windows路径	通过	[ ]	
E2E	002	跨平台文件锁	并发写入	无冲突	[ ]	
High	001	无macOS runner	grep	0命中	[ ]	
High	002	免费额度内	估算<2000分钟	是	[ ]	

地狱收卷红线（10项质量优先）：

1. RED-W2-001：Windows CI失败 → 失败
2. RED-W2-002：覆盖率门禁<80%未阻断 → 失败
3. RED-W2-003：性能回归>10%未阻断 → 失败
4. RED-W2-004：使用macOS runner → 失败
5. RED-W2-005：原116测试在CI中失败 → 失败
6. RED-W2-006：未配置Windows runner → 失败
7. RED-W2-007：使用付费自托管runner → 失败
8. RED-W2-008：CI配置<100行（过于简单）→ 失败
9. RED-W2-009：无回归历史趋势 → 失败
10. RED-W2-010：跨平台适配层缺失 → 失败

Wave 2验收标准：
- 13项刀刃全绿
- 10项红线零触发
- Windows+Linux双平台Actions绿
- 验收口令："Wave 2 A级/Go，启动Wave 3"

---

🔥 Wave 3：选项C - 协议安全 + 向后兼容（依赖Wave 2）

角色：Engineer（质量优先模式）

目标：.hctx格式版本迁移（v2.5/2.6/2.7），输入沙箱（100MB限制/zip bomb检测），数据零丢失

前置条件：Wave 2验收A级/Go

最终验收：Phase 3整体A级/Go

输出交付物：

交付物1：版本迁移器（version-migrator.ts）
- 文件路径：`src/format/version-migrator.ts`
- 行数限制：150行±10行（质量优先，允许严谨设计）
- 必须包含的正则：
  - `v2.5.*v2.6.*v2.7`（版本识别）
  - `.hctx.backup`（备份机制）
  - `BLAKE3`（校验和）
  - `rollback`（回滚机制）

交付物2：输入沙箱（input-sandbox.ts）
- 文件路径：`src/security/input-sandbox.ts`
- 行数限制：120行±10行
- 必须包含：100MB限制、zip bomb检测（100:1压缩比）、递归深度≤10、向量数≤100K、单向量≤10MB

交付物3：向后兼容测试（backward-compat.test.ts）
- 文件路径：`tests/e2e/backward-compat.test.ts`
- 行数限制：80行±10行
- 测试场景：v2.5加载→迁移→校验→v2.7写入→读取一致性

《刀刃》风险自测表（16项质量版）：

类别	编号	自测项	验证命令	通过标准	状态	
FUNC	001	v2.5→v2.7迁移	测试	pass	[ ]	
FUNC	002	v2.6→v2.7迁移	测试	pass	[ ]	
FUNC	003	100MB文件拒绝	测试	throw	[ ]	
FUNC	004	zip bomb检测	恶意文件	拒绝	[ ]	
CONST	001	迁移失败备份	模拟失败	.backup存在	[ ]	
CONST	002	校验和验证	BLAKE3	命中	[ ]	
NEG	001	损坏文件处理	篡改字节	不崩溃	[ ]	
NEG	002	中断恢复	中断后重试	可恢复	[ ]	
NEG	003	原116测试	`npm test`	116 passed	[ ]	
E2E	001	跨版本SHA一致	迁移后校验	一致	[ ]	
E2E	002	大数据迁移	10K向量	成功	[ ]	
E2E	003	并发迁移	多文件	无冲突	[ ]	
High	001	数据零丢失	对比工具	100%	[ ]	
High	002	恶意文件不崩溃	fuzzing	不崩溃	[ ]

地狱收卷红线（10项质量优先）：
1. 
RED-W3-001：v2.5文件无法迁移（数据丢失）→ 失败
2. 
RED-W3-002：迁移失败未保留备份 → 失败
3. 
RED-W3-003：100MB限制未生效 → 失败
4. 
RED-W3-004：zip bomb未检测 → 失败
5. 
RED-W3-005：原116测试被破坏 → 失败
6. 
RED-W3-006：向后兼容测试缺失 → 失败
7. 
RED-W3-007：无校验和验证 → 失败
8. 
RED-W3-008：无回滚机制 → 失败
9. 
RED-W3-009：数据丢失>0% → 失败
10. 
RED-W3-010：代码<100行（过于简单）→ 失败

Wave 3验收标准：
 
14项刀刃全绿
 
10项红线零触发
 
v2.5/v2.6迁移测试通过
 
恶意文件防护验证通过
 
最终口令："Phase 3 A级/Go，v2.8.0-PROD-HARDENED达成"

【串行验收铁律】（质量优先核心）

Wave 1 (A级?) ──否──→ 返工重来（不计时间成本）
    │
    是（10红线零触发+13刀刃全绿）
    ▼
Wave 2 (A级?) ──否──→ 返工重来（不计时间成本）
    │
    是（10红线零触发+13刀刃全绿）
    ▼
Wave 3 (A级?) ──否──→ 返工重来（不计时间成本）
    │
    是（10红线零触发+14刀刃全绿）
    ▼
Phase 3 A级/Go，v2.8.0-PROD-HARDENED

熔断规则：任一波红线触发，立即停止后续波次，当前波次返工至全绿。

【P4自测轻量检查表】（每波收卷前强制）

检查点	自检问题	覆盖情况	相关用例ID	
核心功能	当前波次核心功能是否完整？	[ ]	FUNC-001004	
约束回归	原116测试是否保持？	[ ]	NEG-003	
负面路径	边界/异常/攻击是否处理？	[ ]	NEG/High	
用户体验	输出是否可观测/可诊断？	[ ]	日志/报告	
端到端	完整流程是否通顺？	[ ]	E2E	
高风险	数据丢失/安全绕过是否防护？	[ ]	High	
字段完整	刀刃表是否全部手动勾选？	[ ]	全表	
映射正确	是否关联前波成果？	[ ]	串行依赖	
执行处理	是否有Fail项未处理？	[ ]	必须全绿	
质量优先	是否因时间而妥协质量？	[ ]	禁止妥协

【收卷格式】（每波强制·三波统一）

## ✅ Wave X/3 完成并提交（X=1,2,3）

### 提交信息
- Commit: `feat(phase3-waveX): [描述]`
- 分支: `feat/phase3-prod-hardening`
- 变更文件: [列表]

### 自测报告路径
`docs/self-audit/phase3-waveX/WAVE-X-SELF-AUDIT.md`

### 刀刃表摘要
| 类别 | 覆盖数 | 关键证据 |
|:---|:---:|:---|
| FUNC | X/X | ... |
| ... | ... | ... |

### 地狱红线验证（10项）
| 红线ID | 状态 | 证据 |
|:---|:---:|:---|
| RED-WX-001 | ✅/❌ | ... |
| ... | ... | ... |

### P4检查表
10/10项已覆盖。

### 债务声明
- [当前波次债务]
- 前置债务：Wave X-1已清零（如适用）

### 验收申请
申请Wave X验收审计，目标A级/Go。

📢 派单口令（串行质量版）
Engineer，Phase 3生产硬化三波串行启动！
轰炸模式：Wave 1 → Wave 2 → Wave 3，前波不过后波不开，任一波10红线触发立即返工重来
质量原则：不计时间成本，只求硬核质量，3分钟压力也要严谨，CI配置也要完整，数据迁移零丢失
实用主义边界：3分钟（非7×24），Windows/Linux（非macOS），但质量不妥协
Wave 1（当前）：3分钟压力测试硬化，RSS<30%，10红线，13刀刃
Wave 2（依赖W1）：CI/CD+跨平台，Windows/Linux双绿，覆盖率门禁，10红线，13刀刃
Wave 3（依赖W2）：协议安全+向后兼容，v2.5迁移零丢失，zip bomb检测，10红线，14刀刃
串行铁律：
 
Wave 1 A级 → 启动Wave 2
 
Wave 2 A级 → 启动Wave 3
 
Wave 3 A级 → Phase 3完成，v2.8.0-PROD-HARDENED

Ouroboros Phase 3串行咬合，实验室A级→生产Hardened，质量优先，前波不过后波不开，推！ ☝️🐍♾️🔒💪⚔️